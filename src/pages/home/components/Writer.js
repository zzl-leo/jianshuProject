import React from 'react'
import {WriterWrapper} from '../styled'
class Writer extends React.Component {
    render () {
        return (
            <WriterWrapper>Writer---</WriterWrapper>
        )
    }
}

export default Writer